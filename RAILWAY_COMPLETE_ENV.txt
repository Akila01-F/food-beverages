# Complete Environment Variables for Railway Web App
# Copy these exact variables to your Railway Dashboard → food-beverages service → Variables tab

# Laravel Application Configuration
APP_NAME="Food & Beverage App"
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:Ia0dY4/MOZwXB2ZReig3oMqsEhVghC05FXdCCPpjtFU=
APP_TIMEZONE=UTC
APP_URL=${{RAILWAY_PUBLIC_DOMAIN}}

# Database Configuration (PostgreSQL)
DB_CONNECTION=pgsql
DB_URL=${{PostgreSQL.DATABASE_URL}}
DB_HOST=${{PostgreSQL.PGHOST}}
DB_PORT=${{PostgreSQL.PGPORT}}
DB_DATABASE=${{PostgreSQL.PGDATABASE}}
DB_USERNAME=${{PostgreSQL.PGUSER}}
DB_PASSWORD=${{PostgreSQL.PGPASSWORD}}

# MongoDB Configuration
MONGODB_DSN=${{MongoDB.MONGO_URL}}
MONGO_DB_HOST=${{MongoDB.MONGOHOST}}
MONGO_DB_PORT=${{MongoDB.MONGOPORT}}
MONGO_DB_DATABASE=food_beverage_mongo
MONGO_DB_USERNAME=${{MongoDB.MONGOUSER}}
MONGO_DB_PASSWORD=${{MongoDB.MONGOPASSWORD}}

# Laravel Framework Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=false

CACHE_STORE=database
QUEUE_CONNECTION=database

# Logging Configuration
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Mail Configuration (using log driver for now)
MAIL_MAILER=log
MAIL_HOST=null
MAIL_PORT=null
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@example.com
MAIL_FROM_NAME="${APP_NAME}"

# Broadcasting & Real-time Features
BROADCAST_CONNECTION=log
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# File Storage
FILESYSTEM_DISK=local

# Security & Authentication
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1,127.0.0.1:8000,::1,${{RAILWAY_PUBLIC_DOMAIN}}

# Additional Laravel Configuration
MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Development/Debug (only for troubleshooting, set to false for production)
APP_DEBUG=false
LOG_LEVEL=error